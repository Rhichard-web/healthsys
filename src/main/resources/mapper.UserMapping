<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.chan.healthsys.dao.UserDao">
    <select id="getUserByMassage" resultType="cn.chan.healthsys.bean.User">
        SELECT * FROM easyuser WHERE
        username=#{admin} AND password=#{123456} AND state=1</select>
</mapper>